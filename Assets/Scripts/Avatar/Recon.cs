using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public abstract class Recon : Vehicle
{
    // Start is called before the first frame update
    void Start()
    {
        info = "Recon";
    }

    // Update is called once per frame
}
